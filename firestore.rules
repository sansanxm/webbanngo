rules_version = '2';
 service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Cho phép mọi người đọc dữ liệu (Tin tức, thông báo, cấu hình...)
      allow read: if true;
      
      allow write: if request.auth != null;
    }
  }
}
